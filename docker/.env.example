# Copy to docker/.env (gitignored) before `docker compose up`.
# These must match ES_DATABASE_URL in the project root .env.
ES_PG_USER=es
ES_PG_PASSWORD=change_me_dev_only
ES_PG_DB=es
ES_PG_PORT=55432
