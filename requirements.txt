fastapi==0.115.6
uvicorn[standard]==0.32.1
sqlalchemy==2.0.36
psycopg[binary]==3.2.3
pgvector==0.3.6
pydantic==2.10.3
pydantic-settings==2.6.1
litellm==1.55.3
langgraph==0.2.60
typer==0.15.1
click<8.2  # typer 0.15.1's rich help formatter breaks on click>=8.2's make_metavar(ctx) signature change
rich==13.9.4
httpx
msgpack==1.2.1
# Pinned to match the docker/tools/browser image tag (v1.61.0-noble) —
# host-side pip package only, the actual browser binary runs inside the
# sandbox container and is connected to over CDP (api/browser_client.py),
# so `playwright install` for browser binaries is not needed here.
playwright==1.61.0
pytest==8.3.4
